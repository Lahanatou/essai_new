<!DOCTYPE html>
<html>
  <head>
    <title>GestOrfD</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <% if user_signed_in? %>
     <%= link_to "Liste des orphelins", orphans_path %>
     <%= link_to "Faites vos dons", orphans_don_path  %>
     <%= link_to "Espace de parrainage", orphans_parrainage_path %>
     <%= link_to "Voulez vous adoptez?", orphans_adoption_path %>
     <%= link_to "Envoyer nous un message", orphans_message_path %>
<%= link_to "Déconnexion", destroy_user_session_path, method: :delete %>

   <% else %>
     <%= link_to "Nouvel enregistrement", new_user_registration_path %>
     <%= link_to "Connexion", new_user_session_path %>
   <% end %>

   <% if notice %>
    <p class="alert alert-notice"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="alert alert-error"><%= alert %></p>
  <% end %>

   <%= yield %>
 </body>
</html>
